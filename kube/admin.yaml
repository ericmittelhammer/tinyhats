apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-service
  template:
    metadata:
      labels:
        app: admin-service
    spec:
      containers:
        - name: admin-service
          image: ${REGISTRY}/tinyhats_reinvent/admin-service
          ports:
            - containerPort: 4040
          env:
            - name: HOST
              value: mysql
            - name: PASSWORD
              value: password
            - name: NEW_RELIC_LICENSE_KEY
              value: "${NEW_RELIC_LICENSE_KEY}"
            - name: NEW_RELIC_METADATA_REPOSITORY_URL
              value: "${REPOSITORY_URL}"
            - name: NEW_RELIC_METADATA_COMMIT
              value: "${COMMIT}"
          imagePullPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: admin-service
spec:
  selector:
    app: admin-service
  ports:
    - port: 80
      targetPort: 4040
  type: ClusterIP